<!DOCTYPE html>
{% include 'navbar.html.twig' %}
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Symfony + Tailwind</title>
    <link rel="stylesheet" href="{{ asset('build/tailwind.css') }}">
    <link href="{{ asset('build/tailwind.css') }}" rel="stylesheet">

</head>
    <div class="bg-gray-100 min-h-screen flex items-center justify-center">
        <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-md">
            <h2 class="text-2xl font-bold text-center text-gray-700 mb-6">Créer un compte</h2>
            {{ form_start(registrationForm) }}

            <!-- Prénom -->
            <div class="mb-4">
                {{ form_label(registrationForm.firstname, 'Prénom', {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                {{ form_widget(registrationForm.firstname, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
                {{ form_errors(registrationForm.firstname) }}
            </div>

            <!-- Nom -->
            <div class="mb-4">
                {{ form_label(registrationForm.lastname, 'Nom', {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                {{ form_widget(registrationForm.lastname, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
                {{ form_errors(registrationForm.lastname) }}
            </div>

            <!-- Email -->
            <div class="mb-4">
                {{ form_label(registrationForm.email, 'Email', {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                {{ form_widget(registrationForm.email, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
                {{ form_errors(registrationForm.email) }}
            </div>

{#            <!-- Date de naissance -->#}
{#            <div class="mb-4">#}
{#                {{ form_label(registrationForm.birthDate, 'Date de naissance', {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}#}
{#                {{ form_widget(registrationForm.birthDate, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}#}
{#                {{ form_errors(registrationForm.birthDate) }}#}
{#            </div>#}

            <!-- Mot de passe -->
            <div class="mb-4">
                {{ form_label(registrationForm.password, 'Mot de passe', {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                {{ form_widget(registrationForm.password, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
                {{ form_errors(registrationForm.password) }}
            </div>

            <!-- Bouton de soumission -->
            <div class="text-center">
                <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md w-full">
                    Créer un compte
                </button>
            </div>

            {{ form_end(registrationForm) }}
        </div>
    </div>
